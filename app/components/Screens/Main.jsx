import React from "react";
import {
  StyleSheet,
  Text,
  View,
  TextInput,
  Image,
  TouchableOpacity,
  ScrollView,
  Alert,
  textInput,
  StatusBar,
  Platform,
  KeyboardAvoidingView,
  SafeAreaView,
  Dimensions,
} from "react-native";

const Main = ({ navigation }) => {
  return <Text>Hello</Text>;
};

export default Main;
